import {NextResponse} from 'next/server'
import {supabaseServer} from '@/lib-supabase-server'
export const runtime='nodejs'
export async function GET(req:Request){try{const supabase=await supabaseServer();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required'},{status:401});const titleId=new URL(req.url).searchParams.get('titleId');if(!titleId)return NextResponse.json({error:'Title is required'},{status:400});const {data,error}=await supabase.from('watch_history').select('progress_seconds,duration_seconds,updated_at,completed').eq('user_id',user.id).eq('title_id',titleId).maybeSingle();if(error)throw error;return NextResponse.json({progressSeconds:data?.progress_seconds??0,durationSeconds:data?.duration_seconds??0,completed:data?.completed??false})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to load progress'},{status:500})}}
export async function POST(req:Request){try{const supabase=await supabaseServer();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required'},{status:401});const body=await req.json();const titleId=String(body.titleId||'');const progress=Math.max(0,Math.floor(Number(body.progressSeconds)||0));const duration=Math.max(0,Math.floor(Number(body.durationSeconds)||0));if(!titleId)return NextResponse.json({error:'Title is required'},{status:400});const completed=duration>0&&progress>=Math.max(0,duration-5);const {error}=await supabase.from('watch_history').upsert({user_id:user.id,title_id:titleId,progress_seconds:progress,duration_seconds:duration,completed,updated_at:new Date().toISOString()},{onConflict:'user_id,title_id'});if(error)throw error;return NextResponse.json({ok:true,completed})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to save progress'},{status:500})}}
