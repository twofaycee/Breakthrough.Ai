import {supabaseServer} from '@/lib-supabase-server'
import Link from 'next/link'

export default async function Account(){
 const {data:{user}}=await supabaseServer().auth.getUser()
 if(!user)return <main className="shell"><nav className="nav"><a className="brand" href="/">BREAKTHROUGH</a></nav><section className="formPage"><div className="eyebrow">ACCOUNT</div><h1>Your account</h1><div className="panel"><p className="muted">Sign in to manage your library, watch progress, and membership.</p><Link className="btn primary" href="/login?next=%2Faccount" style={{display:'inline-block',marginTop:14}}>Sign in</Link></div></section></main>
 return <main className="shell"><nav className="nav"><a className="brand" href="/">BREAKTHROUGH</a><div className="navlinks"><Link href="/movies">Movies</Link><Link href="/series">Series</Link><Link href="/my-list">My List</Link><Link href="/continue-watching">Continue</Link></div><div className="actions"><Link className="btn" href="/">Home</Link></div></nav><section className="section" style={{paddingTop:70}}><div className="eyebrow">ACCOUNT</div><h1>{user.user_metadata?.display_name || user.email?.split('@')[0] || 'Member'}</h1><p className="muted">{user.email}</p><div className="grid" style={{marginTop:30}}><div className="panel"><div className="eyebrow">YOUR LIBRARY</div><h3>My List</h3><p className="muted">Saved titles you want to watch later.</p><Link className="btn" href="/my-list" style={{display:'inline-block',marginTop:12}}>Open My List</Link></div><div className="panel"><div className="eyebrow">YOUR PROGRESS</div><h3>Continue Watching</h3><p className="muted">Resume films across devices.</p><Link className="btn" href="/continue-watching" style={{display:'inline-block',marginTop:12}}>Continue Watching</Link></div><div className="panel"><div className="eyebrow">MEMBERSHIP</div><h3>BREAKTHROUGH Pro</h3><p className="muted">Upgrade for an ad-free experience.</p><Link className="btn primary" href="/pricing" style={{display:'inline-block',marginTop:12}}>View membership</Link></div></div></section><footer className="footer">© 2026 BREAKTHROUGH. Stories worth staying up for.</footer></main>
}
