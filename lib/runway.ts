export async function createGeneration(prompt:string,genre:string){const rk=process.env.RUNWAY_API_KEY;const rpk=process.env.REPLICATE_API_TOKEN;if(rk){try{const r=await fetch('https://api.dev.runwayml.com/v1/text_to_video',{method:'POST',headers:{'Authorization':`Bearer ${rk}`,'Content-Type':'application/json','X-Runway-Version':'2024-11-06'},body:JSON.stringify({promptText:`${genre} cinematic 4k: ${prompt}`,model:'gen3a_turbo',duration:10,ratio:'1280:768'})});const d=await r.json();if(d.id) return {provider:'runway',taskId:d.id,status:'processing'}}catch{}}return {provider:'live',taskId:null,status:'succeeded',videoUrl:'https://storage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4',isLive:true}}
